server:
  port: ${SERVER_PORT:9092}
spring:
  application:
    name: task-service
  config:
    import: "optional:configserver:http://${CONFIG_SERVER_HOST:localhost}:8888/"
  profiles:
    active: default